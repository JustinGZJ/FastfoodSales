<UserControl x:Class="DAQ.HomeView" Foreground="DarkGray"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DAQ"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             xmlns:s="https://github.com/canton7/Stylet"
             d:DesignHeight="600" d:DesignWidth="1000"
              d:DataContext="{d:DesignInstance  local:HomeViewModel}"
             >
    <UserControl.Resources>
        <DataTemplate x:Key="SPEC">
            <md:Card UniformCornerRadius="0.2" Margin="5">
                <DockPanel Width="100">
                    <TextBlock Background="DarkGray" Foreground="White"  HorizontalAlignment="Stretch" DockPanel.Dock="Top" FontWeight="Bold" FontSize="14" Text="{Binding Name}"></TextBlock>
                    <StackPanel Margin="5">
                        <DockPanel Visibility="Collapsed">
                            <TextBlock DockPanel.Dock="Left" Width="40" Foreground="Gray">Upper</TextBlock>
                            <TextBlock HorizontalAlignment="Center" Text="{Binding Upper}"></TextBlock>
                        </DockPanel>
                        <DockPanel Visibility="Collapsed">
                            <TextBlock DockPanel.Dock="Left" Width="40" Foreground="Gray">Lower</TextBlock>
                            <TextBlock HorizontalAlignment="Center">0</TextBlock>
                        </DockPanel>
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Left" Width="40" Foreground="Gray">Value</TextBlock>
                            <TextBlock HorizontalAlignment="Center" Text="{Binding Value}"></TextBlock>
                        </DockPanel>
                        <md:PackIcon 
                                        Height="50" 
                                        Width="50"
                                       
                                        HorizontalAlignment="Center"
                                        Foreground="Yellow"
                                        Kind="SmileyNeutral">
                            <md:PackIcon.Style>
                                <Style TargetType="md:PackIcon">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Result}" Value="-1">
                                            <Setter Property="Kind" Value="SmileySad"/>
                                            <Setter Property="Foreground" Value="Red"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Result}" Value="0">
                                            <Setter Property="Kind" Value="SmileyNeutral"/>
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Result}" Value="1">
                                            <Setter Property="Kind" Value="SmileyHappy"/>
                                            <Setter Property="Foreground" Value="Green"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </md:PackIcon.Style>
                        </md:PackIcon>
                    </StackPanel>
                </DockPanel>
            </md:Card>

        </DataTemplate>
    </UserControl.Resources>
    <md:TransitioningContent OpeningEffect="{md:TransitionEffect Kind=SlideInFromRight, Duration=00:00:0.5}">
        <ScrollViewer VerticalScrollBarVisibility="Hidden">
            <StackPanel>
                <ItemsControl Height="auto" ItemTemplate="{StaticResource SPEC}" ItemsSource="{Binding PortAService.TestSpecs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"></StackPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <ItemsControl Height="auto" ItemTemplate="{StaticResource SPEC}" ItemsSource="{Binding PortBService.TestSpecs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"></StackPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <ItemsControl Height="auto" ItemTemplate="{StaticResource SPEC}" ItemsSource="{Binding CameraService.C1Values}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"></StackPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <ItemsControl Height="auto" ItemTemplate="{StaticResource SPEC}" ItemsSource="{Binding CameraService.C2Values}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"></StackPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <ItemsControl Height="auto" ItemTemplate="{StaticResource SPEC}" ItemsSource="{Binding CameraService.C3Values}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"></StackPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </md:TransitioningContent>
</UserControl>
